eureka:
  instance:
    hostname: ${APP_NAME}.herokuapp.com
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: https://reloadly-gate.herokuapp.com/

server:
  port: 8761
spring:
  application:
    name: eureka-service

