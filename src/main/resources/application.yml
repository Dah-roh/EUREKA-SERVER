eureka:
  instance:
    hostname: ${application.name}.herokuapp.com
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URL}/eureka/

spring:
  application:
    name: ${application.name}=eureka-server

application:
  name: eureka-service

